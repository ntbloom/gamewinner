name: gamewinner
run-name: ${{ github.actor }} running CI for gamewinner
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo "Hello world"
